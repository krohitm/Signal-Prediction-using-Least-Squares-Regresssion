function [fitresult, gof] = createFit(lambdaFinal, Mfinal, J)
%this is the 4th step. we are finding the combination of lambda and M for minimum MSE
%we plot the values of MSE against the values of regularization parameters
%and filter orders
%CREATEFIT(LAMBDAFINAL,MFINAL,J)
%  Create a fit.
%
%  Data for 'untitled fit 1' fit:
%      X Input : lambdaFinal
%      Y Input : Mfinal
%      Z Output: J
%  Output:
%      fitresult : a fit object representing the fit.
%      gof : structure with goodness-of fit info.
%
%  See also FIT, CFIT, SFIT.

%  Auto-generated by MATLAB on 25-Sep-2016 14:44:23


%% Fit: 'untitled fit 1'.
[xData, yData, zData] = prepareSurfaceData( lambdaFinal, Mfinal, J );

% Set up fittype and options.
ft = 'linearinterp';

% Fit model to data.
[fitresult, gof] = fit( [xData, yData], zData, ft, 'Normalize', 'on' );

% Plot fit with data.
figure( 'Name', 'untitled fit 1' );
h = plot( fitresult, [xData, yData], zData );
legend( h, 'MSE', 'J vs. lambda, M', 'Location', 'NorthEast' );
% Label axes
xlabel lambda
ylabel M
zlabel MSE
grid on
colormap hsv
colorbar
view( 56.5, 43.6 );


